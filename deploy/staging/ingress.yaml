apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: staging-daskal-ai-ingress
  namespace: argocd
spec:
  rules:
  - host: staging.daskal.ai
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: daskal-ai-service
            port:
              number: 8080